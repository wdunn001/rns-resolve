"""rns-resolve: human-readable name resolution for Reticulum.

Resolvers are ordinary RNS destinations consulted only for input that never
matched the destination-hash pattern. See CONTRACTS.md for the module
contracts and README.md for the trust model.
"""

__version__ = "0.1.0"

APP_NAME = "rnsresolve"
ASPECT = "query"
REQUEST_PATH = "q"
